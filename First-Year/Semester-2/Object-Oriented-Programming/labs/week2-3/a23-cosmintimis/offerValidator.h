#pragma once
#include "offer.h"

int checkIfAnOfferIsValid(char*, char*, date, int);

int checkIfOfferPriceIsValid(int);

int checkIfOfferDepartureDateIsValid(date);

int checkIfOfferDestinationIsValid(char*);

int checkIfOfferTypeIsValid(char*);